import { Box, Typography } from '@mui/material';

export default function PayrollAllocation() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>Payroll Allocation</Typography>
      <Typography color="text.secondary">Payroll Allocation page placeholder. Month filter is on the top-right.</Typography>
    </Box>
  );
}
