import { Box, Typography } from '@mui/material';

export default function ScheduledRecords() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>Scheduled Records</Typography>
      <Typography color="text.secondary">Scheduled Records page placeholder. Month filter is on the top-right.</Typography>
    </Box>
  );
}
