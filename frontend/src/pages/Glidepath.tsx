import { Box, Typography } from '@mui/material';

export default function Glidepath() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>Glidepath</Typography>
      <Typography color="text.secondary">Glidepath page placeholder. Month filter is on the top-right.</Typography>
    </Box>
  );
}
